/**
 * 공통 유틸리티 모듈
 */
const services = (function () {
    return {
        //좋아요/ 찜
        /*toggleLike(button, e){
            if (!isMember) return this.showToast('로그인이 필요해요.');
            if (e) e.preventDefault();

            const idx = button.dataset.idx;
        },*/
    }
})();